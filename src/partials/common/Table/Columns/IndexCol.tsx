export const ID_COL = {
  title: "ID",
  width: 60,
  render: (value, record, index) => index + 1,
};

export const ID_COL_NO_WIDTH = {
  title: "ID",
  render: (value, record, index) => index + 1,
};
